# Polaris 1.0.0 registry preflight — 2026-09-18

Read-only `docker buildx imagetools inspect` checks covered the actual workflow
destinations: `docker.io/nguywnben/polaris` and `ghcr.io/nguywnben/polaris`.
The repository has no `IMAGE_NAME` variable override (GitHub returned 404 for that
variable); both Docker Hub secret names required by CI are present. Secret values
were not read. Secret presence does not prove publishing credentials remain valid.

| Tag | Docker Hub | GHCR |
| --- | --- | --- |
| `1.0.0` | Not found | Not found |
| `v1.0.0` | Not found | Not found |
| `1.0` | Not found | Not found |
| `1` | Not found | Not found |
| `latest` | Not found | Not found |
| `0.1.0-beta.1` | Present; digest below | Present; same digest |

The absence checks returned explicit `not found`, not authorization or network errors.
The beta manifest was successfully read from both repositories using the same client.
No image was pulled, rebuilt, retagged, deleted or published by these checks. Omni
Gateway repositories have different coordinates and are not overwritten by Polaris.
Recheck the target names immediately before publication; registry state can change.

## Preserved rollback identity

The published beta images in both registries have OCI index digest:

`sha256:ab5da96a42e3affb2fb7bca0d32314ec298085d05e66dede0b0fccaf704dcff0`

Their `linux/amd64` image-manifest digest is:

`sha256:e56f9d6203aacd195433c0707df9a23c2475be773cdd8ab1cb4183837c9a4d70`

Recovery references are therefore:

```text
docker.io/nguywnben/polaris@sha256:ab5da96a42e3affb2fb7bca0d32314ec298085d05e66dede0b0fccaf704dcff0
ghcr.io/nguywnben/polaris@sha256:ab5da96a42e3affb2fb7bca0d32314ec298085d05e66dede0b0fccaf704dcff0
```

The GitHub prerelease is now named `v0.1.0-beta`, but published image tags still use
`0.1.0-beta.1`. The historical tag migration did not rename images or rebuild them.
Pin recovery to a digest and preserve an encrypted pre-upgrade backup. A known image
digest alone is not a database rollback guarantee; follow [the update/rollback
procedure](../updating.md) and the deployment's actual previous digest.

## Publication boundary

There is no existing Polaris `1.0.0` image to delete or move. Authorized candidate
work may prepare and test a local image; pushing either registry or creating the
stable GitHub release still requires separate owner authorization. The new published
image digest can only be recorded after that publication succeeds.
