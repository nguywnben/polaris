# P1.4 Cross-Model Review Reconciliation

Date: 2026-09-09
Reviewed candidate: `a38ca74`
External report: `C:\Users\nben6\Downloads\p1.4-cross-model-review.md`
External verdict: **PASS WITH FINDINGS**
Reconciliation commit: `cb4ee10`

## Decision

P1.4 is accepted. The independent review reported no Critical or High findings. Both actionable
implementation findings were fixed and covered by focused regression tests. The remaining Low and
Informational observations are reflected in the operator contract without creating a new retention
subsystem or expanding the fixed R1 scope.

## Finding Reconciliation

| ID | Severity | Assessment | Resolution |
| --- | --- | --- | --- |
| SEC-P14-01 | Medium | Valid | Backup inspection now fails closed when a non-internal SQLite VIEW or TRIGGER exists. The archive test creates both object types and proves rejection before backup creation. |
| SEC-P14-02 | Low | Valid | Runtime reload now invalidates the response-cache coordinator, clearing local cached responses and coordination metadata so restored state cannot reuse a pre-restore response. The route test verifies the invalidation is awaited. |
| SEC-P14-03 | Low | Valid operational trade-off | The runbook now requires free-space checks and explicit cleanup of obsolete `pre-restore-*` snapshots while retaining a known-good rollback point outside the live volume. Automatic pruning is intentionally excluded because the application cannot determine which operator recovery point is safe to delete. The existing fail-before-mutation behavior remains the safety boundary for insufficient space. |
| SEC-P14-04 | Informational | Valid clarification | The runbook now states that the 12–256 character passphrase boundary is evaluated after NFKC normalization, matching the cryptographic implementation's fail-closed contract. No duplicate validation path was added. |

## Focused Post-Review Verification

- Two new/affected regression tests passed individually.
- The complete affected modules passed: 35 tests across portable backup, backup routes, and response
  cache.
- Ruff lint and format checks passed for all four touched Python files.
- `git diff --check` passed before the reconciliation commit.

The fixed task cadence does not require rerunning the already-passed 51-test task gate or isolated
container round trip for these bounded fixes. Their evidence remains attached to the reviewed
candidate in `docs/evidence/p1.4-versioned-backup-restore.md`.
